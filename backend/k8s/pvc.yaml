apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mindspark-data
  namespace: mindspark
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Adjust based on your Pi's storage class
